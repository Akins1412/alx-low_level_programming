#include "main.h"

/**
 * main - prints the alphabet, in lowercase
 *
 * Return: Always 0 (Success)
 */

void print_alphabet(void);
{
	char ch;

	for (ch = 'a'; ch <= 'z'; c++)
		putchar(ch);
	putchar('\n');

	return (0);
}
