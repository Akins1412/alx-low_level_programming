#include <stdio.h>
/**
  * main - Prints the numbers since 0 to 9
  *
  * Return: Always 0 (Success)
  */
int main(void)
{
	int n;

	for (n = '0'; n <= '9'; n++)
	{
		printf(n);
	}

	printf(10);

	return (0);
}
