#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int ch 'a';
	int CH 'A';

	while (ch <= 'a')
	{
		putchar(ch);
		ch++;
	}
	while (ch <= 'A')
	{
		putchar(CH);
		CH++;
	}
	putchar('\n')
	return (0);
}
